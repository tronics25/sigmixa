import { advancePlayback, buildTrajectoryPoints, defaultTrajectoryView, presetTrajectoryView, projectPoint3d, trailAt, type TrajectoryPoint, type TrajectoryViewTransform } from '../../core/trajectory/trajectory';
import type { LogViewState, SignalGroupDto, SignalSeriesDto, ToExtensionMessage, ToWebviewMessage } from '../../extension/editors/rawLogProtocol';
import type { SignalDefinition } from '../../core/signal/signal';
import type { TimelineController } from '../shared/timelineController';

export class TrajectoryView {
  private definitions = new Map<string, SignalDefinition>(); private groups: readonly SignalGroupDto[] = []; private groupBySignal = new Map<string, string>();
  private xId?: string; private yId?: string; private zId?: string; private invert: { x: boolean; y: boolean; z: boolean }; private equalAspect: boolean;
  private trail: 'all' | 'to-current' | 'last-seconds'; private trailSeconds: number; private speed: 0.25 | 0.5 | 1 | 2 | 5; private view: TrajectoryViewTransform;
  private readonly canvas: HTMLCanvasElement; private readonly search: HTMLInputElement; private series: readonly SignalSeriesDto[] = []; private points: readonly TrajectoryPoint[] = [];
  private visible = false; private catalogRequest = 0; private seriesRequest = 0; private counter = 300_000; private current?: number; private playing = false; private animation?: number; private lastTick?: number;

  constructor(private readonly host: HTMLElement, initial: LogViewState | undefined, private readonly post: (message: ToExtensionMessage) => void, private readonly save: (partial: Partial<LogViewState>) => void, private readonly timeline: TimelineController) {
    this.xId = initial?.trajectoryXId; this.yId = initial?.trajectoryYId; this.zId = initial?.trajectoryZId; this.invert = { x: false, y: false, z: false, ...initial?.trajectoryInvert };
    this.equalAspect = initial?.trajectoryEqualAspect ?? true; this.trail = initial?.trajectoryTrail ?? 'all'; this.trailSeconds = initial?.trajectoryTrailSeconds ?? 10; this.speed = initial?.trajectorySpeed ?? 1; this.view = initial?.trajectoryView ?? defaultTrajectoryView();
    host.innerHTML = `<div class="trajectory-layout"><aside class="trajectory-pane"><input class="trajectory-search" type="search" placeholder="Search Signals…" aria-label="Search trajectory Signals"><label>X axis<select class="axis-x"></select></label><label>Y axis<select class="axis-y"></select></label><label>Z axis <small>(optional)</small><select class="axis-z"></select></label><button class="axis-swap">Swap X ↔ Y</button><div class="trajectory-checks"><label><input class="invert-x" type="checkbox"> Invert X</label><label><input class="invert-y" type="checkbox"> Invert Y</label><label><input class="invert-z" type="checkbox"> Invert Z</label><label><input class="equal-aspect" type="checkbox"> Equal aspect</label></div><label>Trail<select class="trail-mode"><option value="all">All</option><option value="to-current">To current time</option><option value="last-seconds">Last N seconds</option></select></label><label class="trail-seconds-label">Trail seconds<input class="trail-seconds" type="number" min="0" step="1"></label></aside><section class="trajectory-main"><div class="view-toolbar"><strong class="trajectory-mode">2D</strong><button class="preset-top">Top</button><button class="preset-front">Front</button><button class="preset-side">Side</button><button class="preset-reset">Reset View</button><span class="spacer"></span><span class="trajectory-status muted">Choose X and Y Signals</span></div><div class="trajectory-canvas-wrap"><canvas></canvas></div><div class="trajectory-playback"><button class="play-start" title="Go to start">|◀</button><button class="play-toggle">▶</button><input class="play-seek" type="range" min="0" max="1" step="any" value="0" aria-label="Trajectory time"><output class="play-time">—</output><select class="play-speed" aria-label="Playback speed"><option value="0.25">0.25×</option><option value="0.5">0.5×</option><option value="1">1×</option><option value="2">2×</option><option value="5">5×</option></select></div></section></div>`;
    this.canvas = host.querySelector('canvas')!; this.search = host.querySelector('.trajectory-search')!; this.bind(); this.applyControls();
    this.timeline.subscribe((timestamp, source) => { if (source === 'trajectory' || !this.points.length) return; this.current = clamp(timestamp, this.points[0].timestamp, this.points.at(-1)!.timestamp); this.updatePlayback(); this.draw(); });
    new ResizeObserver(() => this.draw()).observe(host.querySelector('.trajectory-canvas-wrap')!);
  }

  show(): void { this.visible = true; if (!this.catalogRequest) { this.catalogRequest = ++this.counter; this.post({ type: 'signalCatalogRequest', requestId: this.catalogRequest, view: 'trajectory' }); } this.draw(); }
  hide(): void { this.visible = false; this.pause(); }
  handle(message: ToWebviewMessage): void {
    if (message.type === 'signalCatalog' && message.requestId === this.catalogRequest) { this.setCatalog(message.definitions, message.groups); this.request(); }
    else if (message.type === 'signalSeries' && message.requestId === this.seriesRequest) { this.series = message.series; this.rebuildPoints(); }
    else if (message.type === 'definitionsChanged' || message.type === 'analysisComplete') { this.catalogRequest = 0; this.series = []; this.points = []; if (this.visible) this.show(); }
  }

  private bind(): void {
    this.search.addEventListener('input', () => this.renderAxisOptions());
    for (const [axis, selector] of [['x', '.axis-x'], ['y', '.axis-y'], ['z', '.axis-z']] as const) (this.host.querySelector(selector) as HTMLSelectElement).addEventListener('change', (event) => { (this as any)[`${axis}Id`] = (event.target as HTMLSelectElement).value || undefined; if (axis === 'x') this.clearIncompatibleAxes(); this.renderAxisOptions(); this.persist(); this.request(); });
    this.host.querySelector('.axis-swap')!.addEventListener('click', () => { [this.xId, this.yId] = [this.yId, this.xId]; [this.invert.x, this.invert.y] = [this.invert.y, this.invert.x]; this.renderAxisOptions(); this.applyControls(); this.persist(); this.request(); });
    for (const axis of ['x', 'y', 'z'] as const) (this.host.querySelector(`.invert-${axis}`) as HTMLInputElement).addEventListener('change', (event) => { this.invert[axis] = (event.target as HTMLInputElement).checked; this.persist(); this.rebuildPoints(); });
    (this.host.querySelector('.equal-aspect') as HTMLInputElement).addEventListener('change', (event) => { this.equalAspect = (event.target as HTMLInputElement).checked; this.persist(); this.draw(); });
    (this.host.querySelector('.trail-mode') as HTMLSelectElement).addEventListener('change', (event) => { this.trail = (event.target as HTMLSelectElement).value as typeof this.trail; this.applyControls(); this.persist(); this.draw(); });
    (this.host.querySelector('.trail-seconds') as HTMLInputElement).addEventListener('change', (event) => { this.trailSeconds = Math.max(0, Number((event.target as HTMLInputElement).value) || 0); this.persist(); this.draw(); });
    for (const preset of ['top', 'front', 'side', 'reset'] as const) this.host.querySelector(`.preset-${preset}`)!.addEventListener('click', () => { this.view = presetTrajectoryView(preset); this.persist(); this.draw(); });
    this.host.querySelector('.play-start')!.addEventListener('click', () => this.seek(this.points[0]?.timestamp)); this.host.querySelector('.play-toggle')!.addEventListener('click', () => this.playing ? this.pause() : this.play());
    (this.host.querySelector('.play-seek') as HTMLInputElement).addEventListener('input', (event) => this.seek(Number((event.target as HTMLInputElement).value)));
    (this.host.querySelector('.play-speed') as HTMLSelectElement).addEventListener('change', (event) => { this.speed = Number((event.target as HTMLSelectElement).value) as typeof this.speed; this.persist(); });
    this.bindCanvas();
  }

  private bindCanvas(): void {
    let dragging = false; let middle = false; let previous = { x: 0, y: 0 };
    this.canvas.addEventListener('mousedown', (event) => { if (!this.zId) return; event.preventDefault(); dragging = true; middle = event.button === 1; previous = { x: event.clientX, y: event.clientY }; });
    window.addEventListener('mousemove', (event) => { if (!dragging) return; const dx = event.clientX - previous.x; const dy = event.clientY - previous.y; previous = { x: event.clientX, y: event.clientY }; this.view = middle ? { ...this.view, panX: this.view.panX + dx / 100, panY: this.view.panY - dy / 100 } : { ...this.view, yaw: this.view.yaw + dx * .01, pitch: this.view.pitch + dy * .01 }; this.draw(); });
    window.addEventListener('mouseup', () => { if (dragging) this.persist(); dragging = false; });
    this.canvas.addEventListener('wheel', (event) => { if (!this.zId) return; event.preventDefault(); this.view = { ...this.view, zoom: clamp(this.view.zoom * Math.exp(-event.deltaY * .001), .1, 20) }; this.persist(); this.draw(); }, { passive: false });
  }

  private setCatalog(definitions: readonly SignalDefinition[], groups: readonly SignalGroupDto[]): void { this.definitions = new Map(definitions.map((item) => [item.id, item])); this.groups = groups.filter((group) => !group.remove); this.groupBySignal.clear(); for (const group of this.groups) for (const id of group.signalIds) this.groupBySignal.set(id, group.id); for (const axis of ['xId', 'yId', 'zId'] as const) if (this[axis] && !this.definitions.has(this[axis]!)) this[axis] = undefined; this.clearIncompatibleAxes(); this.renderAxisOptions(); }
  private clearIncompatibleAxes(): void { const group = this.xId ? this.groupBySignal.get(this.xId) : undefined; if (this.yId && this.groupBySignal.get(this.yId) !== group) this.yId = undefined; if (this.zId && this.groupBySignal.get(this.zId) !== group) this.zId = undefined; }
  private renderAxisOptions(): void { const query = this.search.value.trim().toLowerCase(); const xGroup = this.xId ? this.groupBySignal.get(this.xId) : undefined; for (const [axis, selector] of [['x', '.axis-x'], ['y', '.axis-y'], ['z', '.axis-z']] as const) { const select = this.host.querySelector(selector) as HTMLSelectElement; const selected = (this as any)[`${axis}Id`] as string | undefined; select.replaceChildren(); const blank = document.createElement('option'); blank.value = ''; blank.textContent = axis === 'z' ? 'None — 2D' : 'Choose Signal…'; select.appendChild(blank); for (const group of this.groups) { if (axis !== 'x' && xGroup && group.id !== xGroup) continue; const options = group.signalIds.map((id) => this.definitions.get(id)).filter((item): item is SignalDefinition => Boolean(item) && (!query || `${item!.name} ${item!.unit ?? ''}`.toLowerCase().includes(query))); if (!options.length) continue; const optgroup = document.createElement('optgroup'); optgroup.label = group.label; for (const definition of options) { const option = document.createElement('option'); option.value = definition.id; option.textContent = `${definition.name}${definition.unit ? ` (${definition.unit})` : ''}`; option.selected = definition.id === selected; optgroup.appendChild(option); } select.appendChild(optgroup); } } this.updateMode(); }
  private request(): void { if (!this.visible || !this.xId || !this.yId) { this.series = []; this.points = []; this.draw(); return; } const selectedIds = [this.xId, this.yId, ...(this.zId ? [this.zId] : [])]; this.seriesRequest = ++this.counter; this.post({ type: 'signalSeriesRequest', requestId: this.seriesRequest, selectedIds, maxPoints: 10_000 }); }
  private rebuildPoints(): void { const byId = new Map(this.series.map((item) => [item.definition.id, item.samples])); this.points = this.xId && this.yId ? buildTrajectoryPoints(byId.get(this.xId) ?? [], byId.get(this.yId) ?? [], this.zId ? byId.get(this.zId) ?? [] : undefined, this.invert, 4000) : []; if (this.points.length) { const start = this.points[0].timestamp; const end = this.points.at(-1)!.timestamp; this.current = clamp(this.current ?? this.timeline.timestamp ?? start, start, end); } else this.current = undefined; this.updatePlayback(); this.updateMode(); this.draw(); }
  private updateMode(): void { this.host.querySelector('.trajectory-mode')!.textContent = this.zId ? '3D' : '2D'; for (const preset of ['top', 'front', 'side'] as const) (this.host.querySelector(`.preset-${preset}`) as HTMLButtonElement).disabled = !this.zId; }
  private applyControls(): void { for (const axis of ['x', 'y', 'z'] as const) (this.host.querySelector(`.invert-${axis}`) as HTMLInputElement).checked = this.invert[axis]; (this.host.querySelector('.equal-aspect') as HTMLInputElement).checked = this.equalAspect; (this.host.querySelector('.trail-mode') as HTMLSelectElement).value = this.trail; (this.host.querySelector('.trail-seconds') as HTMLInputElement).value = String(this.trailSeconds); (this.host.querySelector('.trail-seconds-label') as HTMLElement).hidden = this.trail !== 'last-seconds'; (this.host.querySelector('.play-speed') as HTMLSelectElement).value = String(this.speed); }
  private seek(timestamp: number | undefined): void { if (timestamp === undefined || !this.points.length) return; this.current = clamp(timestamp, this.points[0].timestamp, this.points.at(-1)!.timestamp); this.timeline.set(this.current, 'trajectory'); this.updatePlayback(); this.draw(); }
  private play(): void { if (!this.points.length) return; if ((this.current ?? 0) >= this.points.at(-1)!.timestamp) this.current = this.points[0].timestamp; this.playing = true; this.lastTick = undefined; this.updatePlayback(); this.animation = requestAnimationFrame((time) => this.tick(time)); }
  private pause(): void { this.playing = false; if (this.animation !== undefined) cancelAnimationFrame(this.animation); this.animation = undefined; this.updatePlayback(); }
  private tick(time: number): void { if (!this.playing || !this.points.length) return; const range = { start: this.points[0].timestamp, end: this.points.at(-1)!.timestamp }; const elapsed = this.lastTick === undefined ? 0 : (time - this.lastTick) / 1000; this.lastTick = time; const next = advancePlayback(this.current ?? range.start, elapsed, this.speed, range); this.seek(next.timestamp); if (next.ended) this.pause(); else this.animation = requestAnimationFrame((nextTime) => this.tick(nextTime)); }
  private updatePlayback(): void { const seek = this.host.querySelector('.play-seek') as HTMLInputElement; const output = this.host.querySelector('.play-time') as HTMLOutputElement; const toggle = this.host.querySelector('.play-toggle') as HTMLButtonElement; toggle.textContent = this.playing ? '❚❚' : '▶'; if (!this.points.length || this.current === undefined) { seek.disabled = true; output.textContent = '—'; return; } seek.disabled = false; seek.min = String(this.points[0].timestamp); seek.max = String(this.points.at(-1)!.timestamp); seek.value = String(this.current); const digits = Math.max(1, Math.ceil(this.points.at(-1)!.timestamp).toString().length); output.style.minWidth = `${digits + 10}ch`; output.textContent = `${fmt(this.current)} / ${fmt(this.points.at(-1)!.timestamp)} s`; }
  private draw(): void { if (!this.visible) return; const wrap = this.canvas.parentElement!; const width = Math.max(320, wrap.clientWidth); const height = Math.max(280, wrap.clientHeight); const ratio = devicePixelRatio || 1; this.canvas.width = width * ratio; this.canvas.height = height * ratio; this.canvas.style.width = `${width}px`; this.canvas.style.height = `${height}px`; const context = this.canvas.getContext('2d')!; context.scale(ratio, ratio); context.clearRect(0, 0, width, height); if (!this.points.length || this.current === undefined) { context.fillStyle = css('--vscode-descriptionForeground', '#888'); context.fillText(this.xId && this.yId ? 'Selected Signals have no exact shared timestamps.' : 'Choose synchronized X and Y Signals.', 20, 30); return; } const trail = trailAt(this.points, this.current, this.trail === 'last-seconds' ? { type: 'last-seconds', seconds: this.trailSeconds } : { type: this.trail }); this.zId ? this.draw3d(context, trail, width, height) : this.draw2d(context, trail, width, height); const point = nearest(this.points, this.current); const names = [this.definitions.get(this.xId!)?.name ?? 'X', this.definitions.get(this.yId!)?.name ?? 'Y', this.zId ? this.definitions.get(this.zId)?.name ?? 'Z' : undefined]; this.host.querySelector('.trajectory-status')!.textContent = point ? `${fmt(point.timestamp)}s · ${names[0]} ${fmt(point.x)} · ${names[1]} ${fmt(point.y)}${point.z === undefined ? '' : ` · ${names[2]} ${fmt(point.z)}`}` : 'No current point'; }
  private draw2d(context: CanvasRenderingContext2D, points: readonly TrajectoryPoint[], width: number, height: number): void { const pad = 36; const all = this.points; const xs = all.map((point) => point.x); const ys = all.map((point) => point.y); const minX = Math.min(...xs); const maxX = Math.max(...xs); const minY = Math.min(...ys); const maxY = Math.max(...ys); const spanX = maxX - minX || 1; const spanY = maxY - minY || 1; const sx = (width - pad * 2) / spanX; const sy = (height - pad * 2) / spanY; const scale = this.equalAspect ? Math.min(sx, sy) : 1; const px = (value: number) => this.equalAspect ? width / 2 + (value - (minX + maxX) / 2) * scale : pad + (value - minX) * sx; const py = (value: number) => this.equalAspect ? height / 2 - (value - (minY + maxY) / 2) * scale : height - pad - (value - minY) * sy; this.strokePath(context, points.map((point) => ({ x: px(point.x), y: py(point.y), t: point.timestamp }))); const current = nearest(points, this.current!); if (current) dot(context, px(current.x), py(current.y)); }
  private draw3d(context: CanvasRenderingContext2D, points: readonly TrajectoryPoint[], width: number, height: number): void { const xs = this.points.map((point) => point.x); const ys = this.points.map((point) => point.y); const zs = this.points.map((point) => point.z ?? 0); const bounds = [xs, ys, zs].map((values) => ({ min: Math.min(...values), max: Math.max(...values) })); const centers = bounds.map((bound) => (bound.min + bound.max) / 2); const span = Math.max(...bounds.map((bound) => bound.max - bound.min), 1); const scale = Math.min(width, height) * .75 / span; const projected = points.map((point) => { const value = projectPoint3d({ x: point.x - centers[0], y: point.y - centers[1], z: (point.z ?? 0) - centers[2] }, this.view); return { x: width / 2 + value.x * scale, y: height / 2 - value.y * scale, t: point.timestamp }; }); this.strokePath(context, projected); const current = projected.length ? nearestProjected(projected, this.current!) : undefined; if (current) dot(context, current.x, current.y); }
  private strokePath(context: CanvasRenderingContext2D, points: readonly { x: number; y: number; t: number }[]): void { if (!points.length) return; for (let index = 1; index < points.length; index++) { const hue = 210 + index / Math.max(1, points.length - 1) * 120; context.strokeStyle = `hsl(${hue} 75% 55%)`; context.lineWidth = 1.8; context.beginPath(); context.moveTo(points[index - 1].x, points[index - 1].y); context.lineTo(points[index].x, points[index].y); context.stroke(); } }
  private persist(): void { this.save({ trajectoryXId: this.xId, trajectoryYId: this.yId, trajectoryZId: this.zId, trajectoryInvert: this.invert, trajectoryEqualAspect: this.equalAspect, trajectoryTrail: this.trail, trajectoryTrailSeconds: this.trailSeconds, trajectorySpeed: this.speed, trajectoryView: this.view }); }
}

function nearest(points: readonly TrajectoryPoint[], timestamp: number): TrajectoryPoint | undefined { return points.reduce<TrajectoryPoint | undefined>((best, point) => !best || Math.abs(point.timestamp - timestamp) < Math.abs(best.timestamp - timestamp) ? point : best, undefined); }
function nearestProjected(points: readonly { x: number; y: number; t: number }[], timestamp: number) { return points.reduce<typeof points[number] | undefined>((best, point) => !best || Math.abs(point.t - timestamp) < Math.abs(best.t - timestamp) ? point : best, undefined); }
function dot(context: CanvasRenderingContext2D, x: number, y: number): void { context.fillStyle = css('--vscode-editorCursor-foreground', '#fff'); context.beginPath(); context.arc(x, y, 5, 0, Math.PI * 2); context.fill(); context.strokeStyle = '#0008'; context.stroke(); }
function clamp(value: number, min: number, max: number): number { return Math.max(min, Math.min(max, value)); }
function fmt(value: number): string { return Number(value.toPrecision(7)).toString(); }
function css(name: string, fallback: string): string { return getComputedStyle(document.documentElement).getPropertyValue(name).trim() || fallback; }
