import { advancePlayback, buildTrajectoryPoints, defaultTrajectoryView, measureTrajectory, presetTrajectoryView, projectPoint3d, trailAt, trajectoryAxisDisplayValue, trajectoryAxisEndpoints, trajectoryAxisScale, type TrajectoryPoint, type TrajectoryViewTransform } from '../../core/trajectory/trajectory';
import type { LogViewState, SignalGroupDto, SignalSeriesDto, ToExtensionMessage, ToWebviewMessage } from '../../extension/editors/rawLogProtocol';
import type { SignalDefinition } from '../../core/signal/signal';
import type { TimelineController } from '../shared/timelineController';
import { t } from '../shared/i18n';
import { crossesSeriesGap } from '../shared/chartSeries';
import { commonDisplayUnit, convertDisplayUnit, type CommonDisplayUnit } from '../../core/units/displayUnit';
import { downsampleEven } from '../../core/timeline/timeline';

export class TrajectoryView {
  private definitions = new Map<string, SignalDefinition>(); private groups: readonly SignalGroupDto[] = []; private groupBySignal = new Map<string, string>();
  private xId?: string; private yId?: string; private zId?: string; private invert: { x: boolean; y: boolean; z: boolean }; private equalAspect: boolean;
  private connectGaps: boolean; private showGrid: boolean; private showPoints: boolean;
  private trail: 'all' | 'to-current' | 'last-seconds'; private trailSeconds: number; private speed: 0.25 | 0.5 | 1 | 2 | 5; private view: TrajectoryViewTransform;
  private readonly canvas: HTMLCanvasElement; private readonly search: HTMLInputElement; private series: readonly SignalSeriesDto[] = []; private analysisPoints: readonly TrajectoryPoint[] = []; private points: readonly TrajectoryPoint[] = [];
  private visible = false; private catalogRequest = 0; private seriesRequest = 0; private counter = 300_000; private current?: number; private playing = false; private animation?: number; private lastTick?: number;
  private projectedPoints: readonly ProjectedTrajectoryPoint[] = []; private hoverTimestamp?: number; private canvasDragging = false; private canvasDragMoved = false;
  private coordinateDisplayUnit?: CommonDisplayUnit; private measurementMode = false; private measurementTimestamps: number[] = [];

  constructor(private readonly host: HTMLElement, initial: LogViewState | undefined, private readonly post: (message: ToExtensionMessage) => void, private readonly save: (partial: Partial<LogViewState>) => void, private readonly timeline: TimelineController) {
    this.xId = initial?.trajectoryXId; this.yId = initial?.trajectoryYId; this.zId = initial?.trajectoryZId; this.invert = { x: false, y: false, z: false, ...initial?.trajectoryInvert };
    this.equalAspect = initial?.trajectoryEqualAspect ?? true; this.connectGaps = initial?.trajectoryConnectGaps !== false; this.showGrid = initial?.trajectoryShowGrid !== false; this.showPoints = initial?.trajectoryShowPoints !== false; this.trail = initial?.trajectoryTrail ?? 'all'; this.trailSeconds = initial?.trajectoryTrailSeconds ?? 10; this.speed = initial?.trajectorySpeed ?? 1; this.view = initial?.trajectoryView ?? defaultTrajectoryView();
    host.innerHTML = `<div class="trajectory-layout"><aside class="trajectory-pane"><input class="trajectory-search" type="search" placeholder="${t('Search Signals…', 'Signalを検索…')}" aria-label="${t('Search trajectory Signals', '軌跡用Signalを検索')}"><label>${t('X axis', 'X軸')}<select class="axis-x"></select></label><label>${t('Y axis', 'Y軸')}<select class="axis-y"></select></label><label>${t('Z axis', 'Z軸')} <small>(${t('optional', '任意')})</small><select class="axis-z"></select></label><div class="trajectory-checks"><label><input class="invert-x" type="checkbox"> ${t('Invert X', 'Xを反転')}</label><label><input class="invert-y" type="checkbox"> ${t('Invert Y', 'Yを反転')}</label><label class="invert-z-label"><input class="invert-z" type="checkbox"> ${t('Invert Z', 'Zを反転')}</label><label class="equal-aspect-label"><input class="equal-aspect" type="checkbox"> ${t('Equal aspect', '縦横比を統一')}</label><label><input class="show-grid" type="checkbox"> ${t('Grid values', '数値グリッド')}</label><label><input class="show-points" type="checkbox"> ${t('Data points', 'データ点')}</label></div><label>${t('Trail', '軌跡範囲')}<select class="trail-mode"><option value="all">${t('All', 'すべて')}</option><option value="to-current">${t('To current time', '現在時刻まで')}</option><option value="last-seconds">${t('Last N seconds', '直近N秒')}</option></select></label><label class="trail-seconds-label">${t('Trail seconds', '軌跡の秒数')}<input class="trail-seconds" type="number" min="0" step="1"></label></aside><section class="trajectory-main"><div class="view-toolbar"><strong class="trajectory-mode">2D</strong><label class="chart-option"><input class="trajectory-connect-gaps" type="checkbox">${t('Connect gaps', '欠損を補完')}</label><span class="trajectory-camera-controls"><button class="preset-top">${t('Top', '上面')}</button><button class="preset-front">${t('Front', '正面')}</button><button class="preset-side">${t('Side', '側面')}</button><button class="preset-reset">${t('Reset View', '表示をリセット')}</button></span><button class="save-trajectory-image">${t('Save Image…', '画像を保存…')}</button><span class="trajectory-interaction-hint muted" hidden>${t('Drag: rotate · Shift+drag: pan · Wheel: zoom', 'ドラッグ: 回転 · Shift+ドラッグ: 移動 · ホイール: 拡大縮小')}</span><span class="spacer"></span><span class="trajectory-status muted">${t('Choose X and Y Signals', 'XとYのSignalを選択')}</span></div><div class="trajectory-canvas-wrap"><canvas aria-label="${t('Trajectory. Hover a point to inspect it; click to move the playback position.', '軌跡。点にマウスを合わせて値を確認、クリックで再生位置を移動。')}"></canvas></div><div class="trajectory-playback"><button class="play-start" title="${t('Go to start', '先頭へ移動')}">|◀</button><button class="play-toggle">▶</button><input class="play-seek" type="range" min="0" max="1" step="any" value="0" aria-label="${t('Trajectory time', '軌跡の時刻')}"><output class="play-time">—</output><select class="play-speed" aria-label="${t('Playback speed', '再生速度')}"><option value="0.25">0.25×</option><option value="0.5">0.5×</option><option value="1">1×</option><option value="2">2×</option><option value="5">5×</option></select></div></section></div>`;
    host.querySelector('.save-trajectory-image')!.insertAdjacentHTML('beforebegin', `<button class="trajectory-measure-toggle" aria-pressed="false">${t('Measure 2 Points', '2点計測')}</button>`);
    host.querySelector('.trajectory-canvas-wrap')!.insertAdjacentHTML('beforebegin', `<div class="trajectory-measurement" hidden><strong>A → B</strong><span class="trajectory-measurement-value"></span><span class="spacer"></span><button class="trajectory-measure-clear">${t('Clear Measurement', '計測を解除')}</button></div>`);
    this.canvas = host.querySelector('canvas')!; this.search = host.querySelector('.trajectory-search')!; this.bind(); this.applyControls(); this.updateMode(); this.updatePlayback(); this.updateMeasurementControls();
    this.timeline.subscribe((timestamp, source) => { if (source === 'trajectory' || !this.points.length) return; this.current = clamp(timestamp, this.points[0].timestamp, this.points.at(-1)!.timestamp); this.updatePlayback(); this.draw(); });
    new ResizeObserver(() => this.draw()).observe(host.querySelector('.trajectory-canvas-wrap')!);
  }

  show(): void { this.visible = true; if (!this.catalogRequest) { this.catalogRequest = ++this.counter; this.post({ type: 'signalCatalogRequest', requestId: this.catalogRequest, view: 'trajectory' }); } this.draw(); }
  hide(): void { this.visible = false; this.pause(); }
  handle(message: ToWebviewMessage): void {
    if (message.type === 'signalCatalog' && message.requestId === this.catalogRequest) { this.setCatalog(message.definitions, message.groups); this.request(); }
    else if (message.type === 'signalSeries' && message.requestId === this.seriesRequest) { this.series = message.series; this.rebuildPoints(); }
    else if (message.type === 'definitionsChanged') { this.catalogRequest = 0; this.series = []; this.analysisPoints = []; this.points = []; this.pause(); this.updatePlayback(); if (this.visible) this.show(); }
  }

  private bind(): void {
    this.search.addEventListener('input', () => this.renderAxisOptions());
    for (const [axis, selector] of [['x', '.axis-x'], ['y', '.axis-y'], ['z', '.axis-z']] as const) (this.host.querySelector(selector) as HTMLSelectElement).addEventListener('change', (event) => { this.setAxisId(axis, (event.target as HTMLSelectElement).value || undefined); if (axis === 'x') this.clearIncompatibleAxes(); this.renderAxisOptions(); this.persist(); this.request(); });
    for (const axis of ['x', 'y', 'z'] as const) (this.host.querySelector(`.invert-${axis}`) as HTMLInputElement).addEventListener('change', (event) => { this.invert[axis] = (event.target as HTMLInputElement).checked; this.persist(); this.rebuildPoints(); });
    (this.host.querySelector('.equal-aspect') as HTMLInputElement).addEventListener('change', (event) => { this.equalAspect = (event.target as HTMLInputElement).checked; this.persist(); this.draw(); });
    (this.host.querySelector('.trajectory-connect-gaps') as HTMLInputElement).addEventListener('change', (event) => { this.connectGaps = (event.target as HTMLInputElement).checked; this.persist(); this.updateMeasurementControls(); this.draw(); });
    (this.host.querySelector('.show-grid') as HTMLInputElement).addEventListener('change', (event) => { this.showGrid = (event.target as HTMLInputElement).checked; this.persist(); this.draw(); });
    (this.host.querySelector('.show-points') as HTMLInputElement).addEventListener('change', (event) => { this.showPoints = (event.target as HTMLInputElement).checked; this.persist(); this.draw(); });
    (this.host.querySelector('.trail-mode') as HTMLSelectElement).addEventListener('change', (event) => { this.trail = (event.target as HTMLSelectElement).value as typeof this.trail; this.applyControls(); this.persist(); this.draw(); });
    (this.host.querySelector('.trail-seconds') as HTMLInputElement).addEventListener('change', (event) => { this.trailSeconds = Math.max(0, Number((event.target as HTMLInputElement).value) || 0); this.persist(); this.draw(); });
    for (const preset of ['top', 'front', 'side', 'reset'] as const) this.host.querySelector(`.preset-${preset}`)!.addEventListener('click', () => { this.view = presetTrajectoryView(preset); this.persist(); this.draw(); });
    this.host.querySelector('.trajectory-measure-toggle')!.addEventListener('click', () => { this.measurementMode = !this.measurementMode; this.updateMeasurementControls(); });
    this.host.querySelector('.trajectory-measure-clear')!.addEventListener('click', () => { this.measurementTimestamps = []; this.updateMeasurementControls(); this.draw(); });
    this.host.querySelector('.save-trajectory-image')!.addEventListener('click', () => this.saveImage());
    this.host.querySelector('.play-start')!.addEventListener('click', () => this.seek(this.points[0]?.timestamp)); this.host.querySelector('.play-toggle')!.addEventListener('click', () => this.playing ? this.pause() : this.play());
    (this.host.querySelector('.play-seek') as HTMLInputElement).addEventListener('input', (event) => this.seek(Number((event.target as HTMLInputElement).value)));
    (this.host.querySelector('.play-speed') as HTMLSelectElement).addEventListener('change', (event) => { this.speed = Number((event.target as HTMLSelectElement).value) as typeof this.speed; this.persist(); });
    this.bindCanvas();
  }

  private bindCanvas(): void {
    let panning = false; let previous = { x: 0, y: 0 };
    this.canvas.addEventListener('mousedown', (event) => { if (!this.zId || (event.button !== 0 && event.button !== 1)) return; event.preventDefault(); this.canvasDragging = true; this.canvasDragMoved = false; panning = event.button === 1 || event.shiftKey; previous = { x: event.clientX, y: event.clientY }; this.canvas.classList.add('dragging'); });
    this.canvas.addEventListener('mousemove', (event) => { if (!this.canvasDragging) this.updateHover(event); });
    this.canvas.addEventListener('mouseleave', () => { if (!this.canvasDragging && this.hoverTimestamp !== undefined) { this.hoverTimestamp = undefined; this.canvas.classList.remove('point-hover'); this.draw(); } });
    this.canvas.addEventListener('click', () => { if (this.canvasDragMoved) { this.canvasDragMoved = false; return; } if (this.hoverTimestamp === undefined) return; if (this.measurementMode) { this.measurementTimestamps = this.measurementTimestamps.length === 1 && this.measurementTimestamps[0] !== this.hoverTimestamp ? [this.measurementTimestamps[0], this.hoverTimestamp] : [this.hoverTimestamp]; this.updateMeasurementControls(); this.draw(); } else this.seek(this.hoverTimestamp); });
    window.addEventListener('mousemove', (event) => { if (!this.canvasDragging) return; const dx = event.clientX - previous.x; const dy = event.clientY - previous.y; previous = { x: event.clientX, y: event.clientY }; if (Math.abs(dx) + Math.abs(dy) > 1) this.canvasDragMoved = true; this.view = panning ? { ...this.view, panX: this.view.panX + dx / 100, panY: this.view.panY - dy / 100 } : { ...this.view, yaw: this.view.yaw + dx * .01, pitch: clamp(this.view.pitch + dy * .01, -Math.PI / 2, Math.PI / 2) }; this.hoverTimestamp = undefined; this.draw(); });
    window.addEventListener('mouseup', () => { if (this.canvasDragging) this.persist(); this.canvasDragging = false; this.canvas.classList.remove('dragging'); });
    this.canvas.addEventListener('wheel', (event) => { if (!this.zId) return; event.preventDefault(); this.view = { ...this.view, zoom: clamp(this.view.zoom * Math.exp(-event.deltaY * .001), .1, 20) }; this.persist(); this.draw(); }, { passive: false });
  }

  private setCatalog(definitions: readonly SignalDefinition[], groups: readonly SignalGroupDto[]): void { this.definitions = new Map(definitions.map((item) => [item.id, item])); this.groups = groups.filter((group) => !group.remove); this.groupBySignal.clear(); for (const group of this.groups) for (const id of group.signalIds) this.groupBySignal.set(id, group.id); for (const axis of ['xId', 'yId', 'zId'] as const) if (this[axis] && !this.definitions.has(this[axis]!)) this[axis] = undefined; this.clearIncompatibleAxes(); this.renderAxisOptions(); }
  private clearIncompatibleAxes(): void { const group = this.xId ? this.groupBySignal.get(this.xId) : undefined; if (this.yId && this.groupBySignal.get(this.yId) !== group) this.yId = undefined; if (this.zId && this.groupBySignal.get(this.zId) !== group) this.zId = undefined; }
  private renderAxisOptions(): void { const query = this.search.value.trim().toLowerCase(); const xGroup = this.xId ? this.groupBySignal.get(this.xId) : undefined; for (const [axis, selector] of [['x', '.axis-x'], ['y', '.axis-y'], ['z', '.axis-z']] as const) { const select = this.host.querySelector(selector) as HTMLSelectElement; const selected = this.axisId(axis); select.replaceChildren(); const blank = document.createElement('option'); blank.value = ''; blank.textContent = axis === 'z' ? t('None — 2D', 'なし — 2D') : t('Choose Signal…', 'Signalを選択…'); select.appendChild(blank); for (const group of this.groups) { if (axis !== 'x' && xGroup && group.id !== xGroup) continue; const options = group.signalIds.map((id) => this.definitions.get(id)).filter((item): item is SignalDefinition => Boolean(item) && (item!.id === selected || !query || `${item!.name} ${item!.unit ?? ''}`.toLowerCase().includes(query))); if (!options.length) continue; const optgroup = document.createElement('optgroup'); optgroup.label = group.label; for (const definition of options) { const option = document.createElement('option'); option.value = definition.id; option.textContent = `${definition.name}${definition.unit ? ` (${definition.unit})` : ''}`; option.selected = definition.id === selected; optgroup.appendChild(option); } select.appendChild(optgroup); } } this.updateMode(); }
  private axisId(axis: 'x' | 'y' | 'z'): string | undefined { return axis === 'x' ? this.xId : axis === 'y' ? this.yId : this.zId; }
  private setAxisId(axis: 'x' | 'y' | 'z', value: string | undefined): void { if (axis === 'x') this.xId = value; else if (axis === 'y') this.yId = value; else this.zId = value; }
  private request(): void { if (!this.visible || !this.xId || !this.yId) { this.series = []; this.analysisPoints = []; this.points = []; this.updatePlayback(); this.draw(); return; } this.pause(); this.host.querySelector('.trajectory-status')!.textContent = t('Loading trajectory…', '軌跡を読み込み中…'); const selectedIds = [this.xId, this.yId, ...(this.zId ? [this.zId] : [])]; this.seriesRequest = ++this.counter; this.post({ type: 'signalSeriesRequest', requestId: this.seriesRequest, selectedIds, maxPoints: 10_000 }); }
  private rebuildPoints(): void {
    const byId = new Map(this.series.map((item) => [item.definition.id, item.samples])); const axisIds = [this.xId, this.yId, this.zId].filter((id): id is string => Boolean(id));
    this.coordinateDisplayUnit = commonDisplayUnit(axisIds.map((id) => this.definitions.get(id)?.unit));
    const samples = (id: string | undefined) => { const source = id ? byId.get(id) ?? [] : []; const unit = id ? this.definitions.get(id)?.unit : undefined; return this.coordinateDisplayUnit ? source.map((sample) => ({ ...sample, value: convertDisplayUnit(sample.value, unit, this.coordinateDisplayUnit!.unit) })) : source; };
    this.analysisPoints = this.xId && this.yId ? buildTrajectoryPoints(samples(this.xId), samples(this.yId), this.zId ? samples(this.zId) : undefined, this.invert, 10_000) : [];
    this.points = downsampleEven(this.analysisPoints, 4000);
    this.measurementTimestamps = this.measurementTimestamps.filter((timestamp) => this.points.some((point) => point.timestamp === timestamp));
    if (this.points.length) { const start = this.points[0].timestamp; const end = this.points.at(-1)!.timestamp; this.current = clamp(this.current ?? this.timeline.timestamp ?? end, start, end); } else this.current = undefined;
    this.updatePlayback(); this.updateMode(); this.updateMeasurementControls(); this.draw();
  }
  private updateMode(): void { const is3d = Boolean(this.zId); this.host.querySelector('.trajectory-mode')!.textContent = is3d ? '3D' : '2D'; (this.host.querySelector('.trajectory-camera-controls') as HTMLElement).hidden = !is3d; (this.host.querySelector('.trajectory-interaction-hint') as HTMLElement).hidden = !is3d; (this.host.querySelector('.invert-z-label') as HTMLElement).hidden = !is3d; (this.host.querySelector('.equal-aspect-label') as HTMLElement).hidden = is3d; this.canvas.classList.toggle('mode-3d', is3d); }
  private applyControls(): void { for (const axis of ['x', 'y', 'z'] as const) (this.host.querySelector(`.invert-${axis}`) as HTMLInputElement).checked = this.invert[axis]; (this.host.querySelector('.equal-aspect') as HTMLInputElement).checked = this.equalAspect; (this.host.querySelector('.trajectory-connect-gaps') as HTMLInputElement).checked = this.connectGaps; (this.host.querySelector('.show-grid') as HTMLInputElement).checked = this.showGrid; (this.host.querySelector('.show-points') as HTMLInputElement).checked = this.showPoints; (this.host.querySelector('.trail-mode') as HTMLSelectElement).value = this.trail; (this.host.querySelector('.trail-seconds') as HTMLInputElement).value = String(this.trailSeconds); (this.host.querySelector('.trail-seconds-label') as HTMLElement).hidden = this.trail !== 'last-seconds'; (this.host.querySelector('.play-speed') as HTMLSelectElement).value = String(this.speed); }
  private seek(timestamp: number | undefined): void { if (timestamp === undefined || !this.points.length) return; this.current = clamp(timestamp, this.points[0].timestamp, this.points.at(-1)!.timestamp); this.timeline.set(this.current, 'trajectory'); this.updatePlayback(); this.draw(); }
  private play(): void { if (!this.points.length) return; if ((this.current ?? 0) >= this.points.at(-1)!.timestamp) this.current = this.points[0].timestamp; this.playing = true; this.lastTick = undefined; this.updatePlayback(); this.animation = requestAnimationFrame((time) => this.tick(time)); }
  private pause(): void { this.playing = false; if (this.animation !== undefined) cancelAnimationFrame(this.animation); this.animation = undefined; this.updatePlayback(); }
  private tick(time: number): void { if (!this.playing || !this.points.length) return; const range = { start: this.points[0].timestamp, end: this.points.at(-1)!.timestamp }; const elapsed = this.lastTick === undefined ? 0 : (time - this.lastTick) / 1000; this.lastTick = time; const next = advancePlayback(this.current ?? range.start, elapsed, this.speed, range); this.seek(next.timestamp); if (next.ended) this.pause(); else this.animation = requestAnimationFrame((nextTime) => this.tick(nextTime)); }
  private updatePlayback(): void { const seek = this.host.querySelector('.play-seek') as HTMLInputElement; const output = this.host.querySelector('.play-time') as HTMLOutputElement; const toggle = this.host.querySelector('.play-toggle') as HTMLButtonElement; const start = this.host.querySelector('.play-start') as HTMLButtonElement; const current = this.current; toggle.textContent = this.playing ? '❚❚' : '▶'; const disabled = !this.points.length || current === undefined; seek.disabled = disabled; toggle.disabled = disabled; start.disabled = disabled; if (disabled) { output.textContent = '—'; return; } seek.min = String(this.points[0].timestamp); seek.max = String(this.points.at(-1)!.timestamp); seek.value = String(current); const digits = Math.max(1, Math.ceil(this.points.at(-1)!.timestamp).toString().length); output.style.minWidth = `${digits + 10}ch`; output.textContent = `${fmt(current)} / ${fmt(this.points.at(-1)!.timestamp)} s`; }
  private draw(): void {
    if (!this.visible) return; const wrap = this.canvas.parentElement!; const width = Math.max(320, wrap.clientWidth); const height = Math.max(280, wrap.clientHeight); const ratio = devicePixelRatio || 1; this.canvas.width = width * ratio; this.canvas.height = height * ratio; this.canvas.style.width = `${width}px`; this.canvas.style.height = `${height}px`; const context = this.canvas.getContext('2d')!; context.scale(ratio, ratio); context.clearRect(0, 0, width, height);
    const saveButton = this.host.querySelector<HTMLButtonElement>('.save-trajectory-image')!; saveButton.disabled = !this.points.length || this.current === undefined;
    if (!this.points.length || this.current === undefined) { this.projectedPoints = []; this.hoverTimestamp = undefined; this.canvas.classList.remove('point-hover'); context.fillStyle = css('--vscode-descriptionForeground', '#888'); context.fillText(this.xId && this.yId ? t('Selected Signals have no exact shared timestamps.', '選択したSignalに一致する時刻がありません。') : t('Choose synchronized X and Y Signals.', '時刻が同期したXとYのSignalを選択してください。'), 20, 30); return; }
    const trail = trailAt(this.points, this.current, this.trail === 'last-seconds' ? { type: 'last-seconds', seconds: this.trailSeconds } : { type: this.trail }); this.zId ? this.draw3d(context, trail, width, height) : this.draw2d(context, trail, width, height); const point = nearest(this.points, this.current);
    this.host.querySelector('.trajectory-status')!.textContent = point ? `${fmt(point.timestamp)} s · ${this.coordinateStatus('x', point.x)} · ${this.coordinateStatus('y', point.y)}${point.z === undefined ? '' : ` · ${this.coordinateStatus('z', point.z)}`}` : t('No current point', '現在位置なし');
  }
  private draw2d(context: CanvasRenderingContext2D, points: readonly TrajectoryPoint[], width: number, height: number): void {
    const pad = { left: 72, right: 32, top: 32, bottom: 56 }; const xScale = trajectoryAxisScale(this.points.map((point) => point.x)); const yScale = trajectoryAxisScale(this.points.map((point) => point.y)); const spanX = xScale.maximum - xScale.minimum; const spanY = yScale.maximum - yScale.minimum; const plotW = Math.max(1, width - pad.left - pad.right); const plotH = Math.max(1, height - pad.top - pad.bottom); const sx = plotW / spanX; const sy = plotH / spanY; const scale = this.equalAspect ? Math.min(sx, sy) : 1; const usedW = this.equalAspect ? spanX * scale : plotW; const usedH = this.equalAspect ? spanY * scale : plotH; const left = pad.left + (plotW - usedW) / 2; const top = pad.top + (plotH - usedH) / 2; const px = (value: number) => left + (value - xScale.minimum) * (this.equalAspect ? scale : sx); const py = (value: number) => top + usedH - (value - yScale.minimum) * (this.equalAspect ? scale : sy);
    this.draw2dAxes(context, xScale, yScale, px, py, left, top, usedW, usedH); const projected = points.map((point) => ({ x: px(point.x), y: py(point.y), depth: 0, t: point.timestamp, point })); this.projectedPoints = projected; this.strokePath(context, projected); if (this.showPoints) paintDataPoints(context, projected); const current = nearest(points, this.current!); if (current) dot(context, px(current.x), py(current.y)); this.paintMeasurement(context); this.paintHover(context, width, height);
  }
  private draw2dAxes(context: CanvasRenderingContext2D, xScale: AxisScale, yScale: AxisScale, px: (value: number) => number, py: (value: number) => number, left: number, top: number, width: number, height: number): void {
    context.save(); context.font = `10px ${css('--vscode-editor-font-family', 'monospace')}`; context.fillStyle = css('--vscode-descriptionForeground', '#888'); context.strokeStyle = css('--vscode-panel-border', '#555'); context.lineWidth = 1;
    if (this.showGrid) {
      context.globalAlpha = .52; for (const tick of xScale.ticks) { const x = px(tick); context.beginPath(); context.moveTo(x, top); context.lineTo(x, top + height); context.stroke(); } for (const tick of yScale.ticks) { const y = py(tick); context.beginPath(); context.moveTo(left, y); context.lineTo(left + width, y); context.stroke(); } context.globalAlpha = 1;
      context.textAlign = 'center'; for (const tick of xScale.ticks) context.fillText(fmt(trajectoryAxisDisplayValue(tick, this.invert.x)), px(tick), top + height + 18); context.textAlign = 'right'; context.textBaseline = 'middle'; for (const tick of yScale.ticks) context.fillText(fmt(trajectoryAxisDisplayValue(tick, this.invert.y)), left - 8, py(tick));
    }
    const xEndpoints = trajectoryAxisEndpoints(xScale.minimum, xScale.maximum, this.invert.x); const yEndpoints = trajectoryAxisEndpoints(yScale.minimum, yScale.maximum, this.invert.y); drawArrow(context, px(xEndpoints.start), py(0), px(xEndpoints.end), py(0), axisColor('x')); drawArrow(context, px(0), py(yEndpoints.start), px(0), py(yEndpoints.end), axisColor('y')); paintAxisLabel(context, px(xEndpoints.end), py(0), this.axisLabel('x'), axisColor('x'), 'x', width + left, height + top); paintAxisLabel(context, px(0), py(yEndpoints.end), this.axisLabel('y'), axisColor('y'), 'y', width + left, height + top); context.restore();
  }
  private draw3d(context: CanvasRenderingContext2D, points: readonly TrajectoryPoint[], width: number, height: number): void {
    const scales = { x: trajectoryAxisScale(this.points.map((point) => point.x)), y: trajectoryAxisScale(this.points.map((point) => point.y)), z: trajectoryAxisScale(this.points.map((point) => point.z ?? 0)) }; const centers = [(scales.x.minimum + scales.x.maximum) / 2, (scales.y.minimum + scales.y.maximum) / 2, (scales.z.minimum + scales.z.maximum) / 2]; const span = Math.max(scales.x.maximum - scales.x.minimum, scales.y.maximum - scales.y.minimum, scales.z.maximum - scales.z.minimum, 1); const scale = Math.min(width, height) * .68 / span; const project = (x: number, y: number, z: number): ProjectedPoint => { const value = projectPoint3d({ x: x - centers[0], y: y - centers[1], z: z - centers[2] }, this.view); return { x: width / 2 + value.x * scale, y: height / 2 - value.y * scale, depth: value.depth, t: 0 }; };
    this.draw3dAxes(context, scales, project, width, height); const projected = points.map((point) => ({ ...project(point.x, point.y, point.z ?? 0), t: point.timestamp, point })); this.projectedPoints = projected; this.strokePath(context, projected); if (this.showPoints) paintDataPoints(context, [...projected].sort((left, right) => left.depth - right.depth)); const current = projected.length ? nearestProjected(projected, this.current!) : undefined; if (current) dot(context, current.x, current.y); this.paintMeasurement(context); this.paintHover(context, width, height);
  }
  private draw3dAxes(context: CanvasRenderingContext2D, scales: Record<'x' | 'y' | 'z', AxisScale>, project: (x: number, y: number, z: number) => ProjectedPoint, width: number, height: number): void {
    context.save(); context.font = `10px ${css('--vscode-editor-font-family', 'monospace')}`; const gridColor = css('--vscode-panel-border', '#555');
    if (this.showGrid) {
      context.strokeStyle = gridColor; context.globalAlpha = .48; for (const tick of scales.x.ticks) drawLine(context, project(tick, scales.y.minimum, 0), project(tick, scales.y.maximum, 0)); for (const tick of scales.y.ticks) drawLine(context, project(scales.x.minimum, tick, 0), project(scales.x.maximum, tick, 0)); context.globalAlpha = 1;
    }
    const xEndpoints = trajectoryAxisEndpoints(scales.x.minimum, scales.x.maximum, this.invert.x); const yEndpoints = trajectoryAxisEndpoints(scales.y.minimum, scales.y.maximum, this.invert.y); const zEndpoints = trajectoryAxisEndpoints(scales.z.minimum, scales.z.maximum, this.invert.z); const axes = [
      { axis: 'x' as const, start: project(xEndpoints.start, 0, 0), end: project(xEndpoints.end, 0, 0), ticks: scales.x.ticks.map((value) => ({ value, point: project(value, 0, 0) })) },
      { axis: 'y' as const, start: project(0, yEndpoints.start, 0), end: project(0, yEndpoints.end, 0), ticks: scales.y.ticks.map((value) => ({ value, point: project(0, value, 0) })) },
      { axis: 'z' as const, start: project(0, 0, zEndpoints.start), end: project(0, 0, zEndpoints.end), ticks: scales.z.ticks.map((value) => ({ value, point: project(0, 0, value) })) },
    ];
    for (const axis of axes) { drawArrow(context, axis.start.x, axis.start.y, axis.end.x, axis.end.y, axisColor(axis.axis)); if (this.showGrid) { context.fillStyle = axisColor(axis.axis); context.textAlign = 'left'; for (const tick of axis.ticks) { if (tick.value === 0 && axis.axis !== 'x') continue; context.fillText(fmt(trajectoryAxisDisplayValue(tick.value, this.invert[axis.axis])), tick.point.x + 4, tick.point.y - 4); } } paintAxisLabel(context, axis.end.x, axis.end.y, this.axisLabel(axis.axis), axisColor(axis.axis), axis.axis, width, height); }
    const origin = project(0, 0, 0); context.fillStyle = css('--vscode-foreground', '#ddd'); context.beginPath(); context.arc(origin.x, origin.y, 3, 0, Math.PI * 2); context.fill(); context.restore();
  }
  private axisUnit(axis: 'x' | 'y' | 'z'): string | undefined { return this.coordinateDisplayUnit?.unit ?? this.definitions.get(this.axisId(axis) ?? '')?.unit; }
  private axisLabel(axis: 'x' | 'y' | 'z'): string { const definition = this.definitions.get(this.axisId(axis) ?? ''); const unit = this.axisUnit(axis); return `${axis.toUpperCase()} · ${definition?.name ?? axis.toUpperCase()}${unit ? ` (${unit})` : ''}`; }
  private coordinateStatus(axis: 'x' | 'y' | 'z', value: number): string { const unit = this.axisUnit(axis); return `${axis.toUpperCase()} ${fmt(trajectoryAxisDisplayValue(value, this.invert[axis]))}${unit ? ` ${unit}` : ''}`; }
  private strokePath(context: CanvasRenderingContext2D, points: readonly { x: number; y: number; t: number }[]): void { if (!points.length) return; for (let index = 1; index < points.length; index++) { if (!this.connectGaps && this.series.some((item) => crossesSeriesGap(item.gaps, points[index - 1].t, points[index].t))) continue; const hue = 210 + index / Math.max(1, points.length - 1) * 120; context.strokeStyle = `hsl(${hue} 75% 55%)`; context.lineWidth = 1.8; context.beginPath(); context.moveTo(points[index - 1].x, points[index - 1].y); context.lineTo(points[index].x, points[index].y); context.stroke(); } }
  private updateHover(event: MouseEvent): void { const bounds = this.canvas.getBoundingClientRect(); if (!bounds.width || !bounds.height || !this.projectedPoints.length) return; const x = (event.clientX - bounds.left) * (this.canvas.clientWidth / bounds.width); const y = (event.clientY - bounds.top) * (this.canvas.clientHeight / bounds.height); let closest: ProjectedTrajectoryPoint | undefined; let distance = 18 * 18; for (const point of this.projectedPoints) { const next = (point.x - x) ** 2 + (point.y - y) ** 2; if (next <= distance) { closest = point; distance = next; } } const timestamp = closest?.t; if (timestamp === this.hoverTimestamp) return; this.hoverTimestamp = timestamp; this.canvas.classList.toggle('point-hover', timestamp !== undefined); this.draw(); }
  private paintMeasurement(context: CanvasRenderingContext2D): void { const selected = this.measurementTimestamps.map((timestamp) => this.projectedPoints.find((item) => item.t === timestamp)).filter((item): item is ProjectedTrajectoryPoint => Boolean(item)); if (!selected.length) return; context.save(); if (selected.length === 2) { context.strokeStyle = css('--vscode-charts-orange', '#d18616'); context.lineWidth = 1.2; context.setLineDash([5, 4]); context.beginPath(); context.moveTo(selected[0].x, selected[0].y); context.lineTo(selected[1].x, selected[1].y); context.stroke(); context.setLineDash([]); } selected.forEach((point, index) => { const color = index === 0 ? css('--vscode-charts-orange', '#d18616') : css('--vscode-charts-blue', '#75beff'); context.fillStyle = css('--vscode-editor-background', '#1e1e1e'); context.strokeStyle = color; context.lineWidth = 2.5; context.beginPath(); context.arc(point.x, point.y, 7, 0, Math.PI * 2); context.fill(); context.stroke(); context.font = `600 11px ${css('--vscode-font-family', 'sans-serif')}`; context.fillStyle = color; context.textAlign = 'center'; context.textBaseline = 'bottom'; context.fillText(index === 0 ? 'A' : 'B', point.x, point.y - 9); }); context.restore(); }
  private updateMeasurementControls(): void { const toggle = this.host.querySelector<HTMLButtonElement>('.trajectory-measure-toggle')!; const panel = this.host.querySelector<HTMLElement>('.trajectory-measurement')!; const value = this.host.querySelector<HTMLElement>('.trajectory-measurement-value')!; const clear = this.host.querySelector<HTMLButtonElement>('.trajectory-measure-clear')!; toggle.classList.toggle('primary', this.measurementMode); toggle.setAttribute('aria-pressed', String(this.measurementMode)); this.canvas.classList.toggle('measurement-mode', this.measurementMode); panel.hidden = !this.measurementMode && !this.measurementTimestamps.length; clear.hidden = !this.measurementTimestamps.length; if (!this.measurementTimestamps.length) value.textContent = t('Select point A on the trajectory.', '軌跡上の始点Aを選択してください。'); else if (this.measurementTimestamps.length === 1) value.textContent = `${t('A selected at', 'Aを選択')} ${fmt(this.measurementTimestamps[0])} s · ${t('Select point B.', '終点Bを選択してください。')}`; else value.textContent = this.measurementSummary() ?? '—'; }
  private measurementSummary(): string | undefined { if (this.measurementTimestamps.length !== 2) return undefined; const measurement = measureTrajectory(this.analysisPoints, this.measurementTimestamps[0], this.measurementTimestamps[1]); if (!measurement) return undefined; const parts = [`Δt ${fmt(measurement.deltaTime)} s`, `ΔX ${fmt(trajectoryAxisDisplayValue(measurement.deltaX, this.invert.x))}${this.axisUnit('x') ? ` ${this.axisUnit('x')}` : ''}`, `ΔY ${fmt(trajectoryAxisDisplayValue(measurement.deltaY, this.invert.y))}${this.axisUnit('y') ? ` ${this.axisUnit('y')}` : ''}`]; if (measurement.deltaZ !== undefined) parts.push(`ΔZ ${fmt(trajectoryAxisDisplayValue(measurement.deltaZ, this.invert.z))}${this.axisUnit('z') ? ` ${this.axisUnit('z')}` : ''}`); if (this.coordinateDisplayUnit) { const unit = this.coordinateDisplayUnit.unit; const start = Math.min(measurement.first.timestamp, measurement.second.timestamp); const end = Math.max(measurement.first.timestamp, measurement.second.timestamp); const incomplete = !this.connectGaps && this.series.some((item) => crossesSeriesGap(item.gaps, start, end)); parts.push(`${t('Straight', '直線距離')} ${fmt(measurement.straightDistance)} ${unit}`); parts.push(incomplete ? `${t('Path', '軌跡長')} — (${t('contains gaps', '欠損あり')})` : `${t('Path', '軌跡長')} ${fmt(measurement.pathLength)} ${unit}`); if (!incomplete && measurement.averageSpeed !== undefined) parts.push(`${t('Average speed', '平均速度')} ${fmt(measurement.averageSpeed)} ${unit}/s`); } return parts.join(' · '); }
  private paintHover(context: CanvasRenderingContext2D, width: number, height: number): void { if (this.hoverTimestamp === undefined) return; const projected = this.projectedPoints.find((item) => item.t === this.hoverTimestamp); if (!projected) return; const lines = [
    { text: `${fmt(projected.t)} s`, color: css('--vscode-foreground', '#ddd') },
    { text: this.coordinateStatus('x', projected.point.x), color: axisColor('x') },
    { text: this.coordinateStatus('y', projected.point.y), color: axisColor('y') },
    ...(projected.point.z === undefined ? [] : [{ text: this.coordinateStatus('z', projected.point.z), color: axisColor('z') }]),
  ]; context.save(); context.font = `11px ${css('--vscode-editor-font-family', 'monospace')}`; const boxWidth = Math.max(...lines.map((line) => context.measureText(line.text).width)) + 16; const boxHeight = lines.length * 17 + 10; const desiredX = projected.x + 14 + boxWidth <= width - 6 ? projected.x + 14 : projected.x - boxWidth - 14; const boxX = clamp(desiredX, 6, Math.max(6, width - boxWidth - 6)); const boxY = clamp(projected.y - boxHeight / 2, 6, Math.max(6, height - boxHeight - 6)); context.fillStyle = css('--vscode-editorHoverWidget-background', '#252526'); context.globalAlpha = .94; context.fillRect(boxX, boxY, boxWidth, boxHeight); context.globalAlpha = 1; context.strokeStyle = css('--vscode-editorHoverWidget-border', '#555'); context.strokeRect(boxX, boxY, boxWidth, boxHeight); context.fillStyle = css('--vscode-editorCursor-foreground', '#fff'); context.beginPath(); context.arc(projected.x, projected.y, 5, 0, Math.PI * 2); context.fill(); context.strokeStyle = '#0009'; context.stroke(); context.textAlign = 'left'; context.textBaseline = 'middle'; lines.forEach((line, index) => { context.fillStyle = line.color; context.fillText(line.text, boxX + 8, boxY + 10 + index * 17 + 8.5); }); context.restore(); }
  private saveImage(): void { if (!this.points.length || this.current === undefined) return; const canvas = document.createElement('canvas'); canvas.width = 1600; canvas.height = 1000; const context = canvas.getContext('2d'); if (!context) return; context.fillStyle = css('--vscode-editor-background', '#1e1e1e'); context.fillRect(0, 0, canvas.width, canvas.height); context.fillStyle = css('--vscode-foreground', '#ddd'); context.font = '600 22px sans-serif'; context.fillText('SigMixa · Trajectory', 24, 34); const mode = this.zId ? '3D' : '2D'; const current = nearest(this.points, this.current); const summary = `${mode}${current ? ` · ${fmt(current.timestamp)} s` : ''}`; context.font = '12px sans-serif'; context.fillStyle = css('--vscode-descriptionForeground', '#999'); context.fillText(summary, canvas.width - context.measureText(summary).width - 24, 32); const scene = document.createElement('canvas'); scene.width = 1600; scene.height = 940; const sceneContext = scene.getContext('2d'); if (!sceneContext) return; sceneContext.fillStyle = css('--vscode-editor-background', '#1e1e1e'); sceneContext.fillRect(0, 0, scene.width, scene.height); const trail = trailAt(this.points, this.current, this.trail === 'last-seconds' ? { type: 'last-seconds', seconds: this.trailSeconds } : { type: this.trail }); const savedHover = this.hoverTimestamp; this.hoverTimestamp = undefined; this.zId ? this.draw3d(sceneContext, trail, scene.width, scene.height) : this.draw2d(sceneContext, trail, scene.width, scene.height); this.hoverTimestamp = savedHover; context.drawImage(scene, 0, 60); this.post({ type: 'saveTrajectoryImage', dataUrl: canvas.toDataURL('image/png') }); this.draw(); }
  private persist(): void { this.save({ trajectoryXId: this.xId, trajectoryYId: this.yId, trajectoryZId: this.zId, trajectoryInvert: this.invert, trajectoryEqualAspect: this.equalAspect, trajectoryConnectGaps: this.connectGaps, trajectoryShowGrid: this.showGrid, trajectoryShowPoints: this.showPoints, trajectoryTrail: this.trail, trajectoryTrailSeconds: this.trailSeconds, trajectorySpeed: this.speed, trajectoryView: this.view }); }
}

function nearest(points: readonly TrajectoryPoint[], timestamp: number): TrajectoryPoint | undefined { return points.reduce<TrajectoryPoint | undefined>((best, point) => !best || Math.abs(point.timestamp - timestamp) < Math.abs(best.timestamp - timestamp) ? point : best, undefined); }
function nearestProjected(points: readonly { x: number; y: number; t: number }[], timestamp: number) { return points.reduce<typeof points[number] | undefined>((best, point) => !best || Math.abs(point.t - timestamp) < Math.abs(best.t - timestamp) ? point : best, undefined); }
function dot(context: CanvasRenderingContext2D, x: number, y: number): void { context.fillStyle = css('--vscode-editorCursor-foreground', '#fff'); context.beginPath(); context.arc(x, y, 5, 0, Math.PI * 2); context.fill(); context.strokeStyle = '#0008'; context.stroke(); }
type AxisScale = ReturnType<typeof trajectoryAxisScale>;
interface ProjectedPoint { readonly x: number; readonly y: number; readonly depth: number; readonly t: number; }
interface ProjectedTrajectoryPoint extends ProjectedPoint { readonly point: TrajectoryPoint; }
function axisColor(axis: 'x' | 'y' | 'z'): string { return axis === 'x' ? css('--vscode-charts-red', '#f14c4c') : axis === 'y' ? css('--vscode-charts-green', '#89d185') : css('--vscode-charts-blue', '#75beff'); }
function drawLine(context: CanvasRenderingContext2D, start: { readonly x: number; readonly y: number }, end: { readonly x: number; readonly y: number }): void { context.beginPath(); context.moveTo(start.x, start.y); context.lineTo(end.x, end.y); context.stroke(); }
function drawArrow(context: CanvasRenderingContext2D, startX: number, startY: number, endX: number, endY: number, color: string): void {
  const angle = Math.atan2(endY - startY, endX - startX); context.save(); context.strokeStyle = color; context.fillStyle = color; context.lineWidth = 1.7; context.beginPath(); context.moveTo(startX, startY); context.lineTo(endX, endY); context.stroke(); context.beginPath(); context.moveTo(endX, endY); context.lineTo(endX - Math.cos(angle - Math.PI / 6) * 8, endY - Math.sin(angle - Math.PI / 6) * 8); context.lineTo(endX - Math.cos(angle + Math.PI / 6) * 8, endY - Math.sin(angle + Math.PI / 6) * 8); context.closePath(); context.fill(); context.restore();
}
function paintAxisLabel(context: CanvasRenderingContext2D, x: number, y: number, text: string, color: string, axis: 'x' | 'y' | 'z', width: number, height: number): void {
  context.save(); context.font = `600 11px ${css('--vscode-font-family', 'sans-serif')}`; const boxWidth = context.measureText(text).width + 10; const boxHeight = 18; const desiredX = x + 7; const desiredY = axis === 'y' ? y - boxHeight - 7 : y + 7; const boxX = clamp(desiredX, 4, Math.max(4, width - boxWidth - 4)); const boxY = clamp(desiredY, 4, Math.max(4, height - boxHeight - 4)); context.globalAlpha = .9; context.fillStyle = css('--vscode-editorHoverWidget-background', '#252526'); context.fillRect(boxX, boxY, boxWidth, boxHeight); context.globalAlpha = 1; context.strokeStyle = color; context.strokeRect(boxX, boxY, boxWidth, boxHeight); context.fillStyle = color; context.textAlign = 'left'; context.textBaseline = 'middle'; context.fillText(text, boxX + 5, boxY + boxHeight / 2); context.restore();
}
function paintDataPoints(context: CanvasRenderingContext2D, points: readonly { readonly x: number; readonly y: number }[]): void {
  if (!points.length) return; const step = Math.max(1, Math.ceil(points.length / 600)); context.save(); context.fillStyle = css('--vscode-charts-purple', '#b180d7'); context.globalAlpha = .62; context.beginPath(); for (let index = 0; index < points.length; index += step) { const point = points[index]; context.moveTo(point.x + 2, point.y); context.arc(point.x, point.y, 2, 0, Math.PI * 2); } context.fill(); context.restore();
}
function clamp(value: number, min: number, max: number): number { return Math.max(min, Math.min(max, value)); }
function fmt(value: number): string { return Number(value.toPrecision(7)).toString(); }
function css(name: string, fallback: string): string { return getComputedStyle(document.documentElement).getPropertyValue(name).trim() || fallback; }
